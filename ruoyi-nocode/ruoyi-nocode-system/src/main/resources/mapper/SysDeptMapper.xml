<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.nocode.system.mapper.SysDeptMapper">

    <resultMap type="com.ruoyi.nocode.system.entity.SysDept" id="SysDeptResult">
        <id property="deptId" column="dept_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="ancestors" column="ancestors"/>
        <result property="deptName" column="dept_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="leader" column="leader"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectDeptVo">
        select d.dept_id, d.parent_id, d.ancestors, d.dept_name, d.order_num, d.leader, d.phone, d.email, d.status,
               d.del_flag, d.create_by, d.create_time, d.update_by, d.update_time, d.remark
        from sys_dept d
    </sql>

    <select id="selectDeptList" parameterType="com.ruoyi.nocode.system.entity.SysDept" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        where d.del_flag = '0'
        <if test="parentId != null">
            AND d.parent_id = #{parentId}
        </if>
        <if test="deptName != null and deptName != ''">
            AND d.dept_name like concat('%', #{deptName}, '%')
        </if>
        <if test="status != null and status != ''">
            AND d.status = #{status}
        </if>
        order by d.parent_id, d.order_num
    </select>

    <select id="selectChildrenDeptById" parameterType="Long" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        where d.del_flag = '0'
        and find_in_set(#{deptId}, d.ancestors)
        order by d.parent_id, d.order_num
    </select>

    <select id="selectDeptById" parameterType="Long" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        where d.dept_id = #{deptId}
    </select>

    <select id="checkDeptExistUser" parameterType="Long" resultType="boolean">
        select case when count(1) > 0 then true else false end
        from sys_user
        where del_flag = '0'
        and dept_id = #{deptId}
    </select>

    <select id="checkDeptNameUnique" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        where d.dept_name = #{deptName} and d.parent_id = #{parentId} and d.del_flag = '0' limit 1
    </select>

    <update id="updateDeptChildren" parameterType="map">
        update sys_dept set ancestors = 
        <foreach collection="newAncestors" item="ancestor" open="concat('" separator="," close="')" >
            #{ancestor}
        </foreach>
        where find_in_set(#{deptId}, ancestors)
    </update>

    <select id="selectDeptListByRoleId" parameterType="Long" resultType="Long">
        select d.dept_id
        from sys_dept d
        left join sys_role_dept rd on d.dept_id = rd.dept_id
        where d.del_flag = '0'
        and rd.role_id = #{roleId}
        order by d.parent_id, d.order_num
    </select>

    <select id="selectDeptByUserId" parameterType="Long" resultMap="SysDeptResult">
        <include refid="selectDeptVo"/>
        where d.dept_id = (
            select dept_id from sys_user where user_id = #{userId}
        )
    </select>

</mapper>
