<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.nocode.system.mapper.SysOperLogMapper">

    <resultMap type="com.ruoyi.nocode.system.entity.SysOperLog" id="SysOperLogResult">
        <id property="operId" column="oper_id"/>
        <result property="title" column="title"/>
        <result property="businessType" column="business_type"/>
        <result property="method" column="method"/>
        <result property="requestMethod" column="request_method"/>
        <result property="operatorType" column="operator_type"/>
        <result property="operName" column="oper_name"/>
        <result property="deptName" column="dept_name"/>
        <result property="operUrl" column="oper_url"/>
        <result property="operIp" column="oper_ip"/>
        <result property="operLocation" column="oper_location"/>
        <result property="operParam" column="oper_param"/>
        <result property="jsonResult" column="json_result"/>
        <result property="status" column="status"/>
        <result property="errorMsg" column="error_msg"/>
        <result property="operTime" column="oper_time"/>
        <result property="delFlag" column="del_flag"/>
    </resultMap>

    <sql id="selectOperLogVo">
        select oper_id, title, business_type, method, request_method, operator_type, oper_name, dept_name,
               oper_url, oper_ip, oper_location, oper_param, json_result, status, error_msg, oper_time, del_flag
        from sys_oper_log
        where del_flag = '0'
    </sql>

    <select id="selectOperLogById" parameterType="Long" resultMap="SysOperLogResult">
        <include refid="selectOperLogVo"/>
        and oper_id = #{operId}
    </select>

    <select id="selectOperLogList" resultMap="SysOperLogResult">
        <include refid="selectOperLogVo"/>
        <if test="operLog != null">
            <if test="operLog.title != null and operLog.title != ''">
                AND title like concat('%', #{operLog.title}, '%')
            </if>
            <if test="operLog.businessType != null and operLog.businessType != ''">
                AND business_type = #{operLog.businessType}
            </if>
            <if test="operLog.status != null">
                AND status = #{operLog.status}
            </if>
            <if test="operLog.operName != null and operLog.operName != ''">
                AND oper_name like concat('%', #{operLog.operName}, '%')
            </if>
        </if>
        <if test="beginTime != null">
            AND oper_time &gt;= #{beginTime}
        </if>
        <if test="endTime != null">
            AND oper_time &lt;= #{endTime}
        </if>
        order by oper_time desc
    </select>

    <delete id="deleteOperLogByIds" parameterType="Long">
        delete from sys_oper_log where oper_id in
        <foreach item="operId" collection="array" open="(" separator="," close=")">
            #{operId}
        </foreach>
    </delete>

    <update id="cleanOperLog">
        truncate table sys_oper_log
    </update>

</mapper>
